name: Manual Approval 7

on:
  pull_request:
    types: [opened, ready-for-review]
    
jobs:
  sendnotificationemail:
    runs-on: ubuntu-latest

    steps:  
    - name: call the logic app to send the notification
      run: curl -X POST -H "Content-Type':' application/json" -d '{"address"':' "pfisher@microsoft.com", "prurl"':' "${{ toJson(github.event)}}", "prtitle"':' "my pull request title"}' 'https://prod-52.eastus2.logic.azure.com:443/workflows/0309a97d290240f185b2c7919f113b16/triggers/manual/paths/invoke?api-version=2016-10-01&sp=%2Ftriggers%2Fmanual%2Frun&sv=1.0&sig=VCZVQ6cr_B4RKpjQSzRL5eeVJAiVFfgBXRI7QYclz2E'
      
